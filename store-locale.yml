server:
  port: ${app.port:9101}

eureka:
  instance:
    hostname: ${app.host:localhost}
    port: ${server.port}
  client:
    serviceUrl:
      defaultZone: http://${registry.host:localhost}:${registry.port:9090}/eureka/
    healthcheck:
      enabled: true

spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: 'a'
        platform: postgres
        url: jdbc:postgresql://${database.host:localhost}:${database.port:5432}/locale?verifyServerCertificate=false&useSSL=false&requireSSL=false
        username: postgres
    jpa:
        database-platform: org.hibernate.dialect.PostgreSQL9Dialect
        show-sql: false
        properties:
            hibernate:
                temp:
                    use_jdbc_metadata_defaults: false
                envers:
                    audit_table_prefix: LOG_
                    audit_table_suffix:
                    revision_field_name: LOG_ID
                    revision_type_field_name: LOG_TYPE
    liquibase:
        enabled: true
        change-log: classpath:/data/changelog/db.changelog-master.xml
