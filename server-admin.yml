server:
  port: ${app.port:9092}

eureka:
  instance:
    hostname: ${registry.host:localhost}
    port: ${registry.port:9090}
  client:
    serviceUrl:
      defaultZone: http://${app.host}:${server.port}/eureka/
  boot:
    admin:
      discovery:
        ignored-services: ${app.name:server-admin}
